# 現在の候補：Goal 014.1 — 音と日本語表示

**DEVICE_ACCEPTANCE=PENDING / RELEASE_READY=false。** 現在のbranchは `fix/goal-014-1-audio-and-player-ui`、作業場所は `/home/mhirotaka/workspace/chroma-rift-goal012`。今回のmarkerは **`goal-014-1-audio-ja-r1`** で、通常ホームには表示しない。`設定 → サポート → 詳しい情報` からcode／preview／native buildを確認する。

今回の手順は末尾の **「Goal 014.1」節** を使う。音楽・環境音・効果音を各エリアで別々に記録し、既存保存を消さない。新しい同梱previewを作成・インストールしてホームアイコンから起動する。Metroで既存同梱previewは更新できない。下のGoal014以前のmarker、ホームでの識別方法、当時の判定は歴史的記録であり、今回の候補確認には使わない。

---

# 過去の記録：Goal014 — 第一章の新しい同梱preview

**DEVICE_ACCEPTANCE=PENDING / RELEASE_READY=false。** コードmarkerは`goal-014-polish-r1`。作業場所は`/home/mhirotaka/workspace/chroma-rift-goal012`、branchは`feat/goal-014-chapter-one-polish`。下のr8以前の失敗履歴・診断手順はそのまま残す。

ユーザー提供のr8 / preview / iOS build3の録画では04から第一章完了へ到達した一経路が確認されている。508.365秒、1170×2532、HEVC/AAC44.1kHz、SHA-256 `db76b663c6ac9d41f85c10f68e86712ab3b604bee9dcc043d2aee0691ca06f9c`。今回の根拠は指定された観察表であり、Codexによる連続視聴・実聴・実機FPS測定ではない。全端末、全5エリア、全再開条件の受入れを意味しない。

新しいnative依存・SDK・設定変更はない。新しい音楽・material・モデルを同梱したpreviewが必要。Metro再読込は既存同梱previewの更新方法ではない。EASはこの作業で実行しない。ユーザーが次を実行する。

```bash
cd /home/mhirotaka/workspace/chroma-rift-goal012 && npx eas-cli@latest build --platform ios --profile preview
```

既存previewはinternal distribution、developmentClient=false、autoIncrement=true、remote appVersionSource。次のbuild番号を推測せず、完成したEAS build IDと端末表示を別々に記録する。起動時に`goal-014-polish-r1 / preview / release-js`と新しいnative番号を確認し、アプリ削除や保存消去はしない。

1. 機内モードで01〜05を通す。各錯視、鈴2個、任意比較、低負荷、文字拡大、簡単操作を確認する。
2. 04はパネルの白いキー取得、実壁のある練習、危険下の本保持、0/3→3/3の確定、鏡で背後確認、片指を離して直ちに左操作で退避。途中取消・pause・background・cold resumeで整数段を保持し、保持指や途中演出は再開しない。
3. 05は手順前の停止対象、キー前の鈴、未収容／一部収容／全身収容／扉sweep／危険側からの操作で、目的・ボタン・理由が一致するか確認する。実鈴に誘導し、扉、停止、在館02→01、職員扉、屋外歩行、01→00を確認する。
4. 屋外到達時点で終了し、cold起動で完了が残ることを確認する。4秒の屋外余韻と12秒の終幕導入は保存を遅らせない。終幕を飛ばす操作には新しい押下が必要。
5. 音楽6曲の主題・状態遷移、足音と重要SEの聞こえ方、イヤホン／スピーカー／モノラルを確認する。音楽・環境音・効果音・消音が保持され、隔離／停止後の追跡音、pause／background／GL失敗後の音漏れがないか確認する。
6. 10回再入場、04鏡描画、retry、旧r8セーブ、標準／控えめを確認する。端末名・OS・設定・長いframe・発熱・音切れを実測の範囲で記録する。失敗時は既存FIRST_FAILUREを使い、画面の準備未完了を成功扱いにしない。

初見3〜5人の理解／手応え／怖さ／不快さ評価はPENDING。公開URL・運営情報・ストア申告も既存の公開ブロッカーのまま。ローカル結果と測定境界は[Goal014 QA](qa-goal014/README.md)へ分離する。

---

# Goal 013 — iPhone previewで第一章を確認する

## Goal 013.1 r8 — r7 build 2のGL失敗を受けた修正版候補

**r7 / preview / release-js / 1.0.0 / iOS build 2 の04受入れはFAIL。新しいr8はPENDING、RELEASE_READY=false。** FIRST_FAILUREを受理し、起動timeoutより前のGL_INVALID_OPERATION (0x502)と判明した。採用Three／Expoのdefault framebufferとBACKの不整合を局所補正し、実software GLES・実Three/鏡scene・native lifecycle/保存回帰を検査した。個々の実機失敗命令は未確定。調査、source hash、全体113 suites／1,190 testsの結果は[GL調査記録](GOAL-013-1-R7-GL-INVESTIGATION.md)を参照する。以下のr7以前の手順・待機理由は歴史的記録として残す。

修正commitは `c79a75f`、新コードmarkerは `goal-013-1-mirror-runtime-r8`。JS/TSだけの変更で、追加native依存や設定変更はない。ただし既存の**同梱r7 previewはMetroでは更新できない**ため、r8を含む新しいpreviewを作成する。ローカル検証後、ユーザーが次を実行する。この作業でこちらはEAS buildを起動していない。

```bash
cd /home/mhirotaka/workspace/chroma-rift-goal012 &&
npx eas-cli@latest build --platform ios --profile preview
```

`preview`はinternal distribution、developmentClient=false、`autoIncrement:true`、`EXPO_PUBLIC_CHROMA_BUILD_PROFILE=preview`。version情報は既存 `appVersionSource:remote` を使い、remote番号をリセットしていない。次のiOS build番号とEAS build IDはここではunknownであり、完成ログと端末で確認する。古いr7や同じbuild 2の再インストールを新候補の確認と扱わない。

1. 新previewのインストールリンクから既存アプリを更新し、ホームのアプリアイコンから直接起動する。第一章ホームで **r8 / preview / iOS build〈新番号〉** を確認し、EAS build IDと別々に記録する。アプリや保存は削除しない。MetroのQRやトンネルはこの同梱previewの更新経路ではない。
2. 既存の「続きから」で04へ入り、最初の3D提示と操作開始を確認する。鍵・確定済み歯止めが以前の進行と整合するか確認し、レバー保持、鏡の正面→視界外／背面→正面、巻き上げ、04出口→05の順で進む。最初から01〜03を再プレイする必要はない。
3. 再発したら「詳細を表示」→「診断情報をコピー」でFIRST_FAILUREを保存する。r8の `firstErrorBoundary`、`firstInvalidOperation`（観測できない場合はnull）、`gl.trace`、`framebufferAdapter`、query failureが次の切り分けに使える。`tracked/requested` はアプリが追跡した論理bindingで、native FBO IDの照会結果ではない。初回traceは上限付きで、成功後は同期command検査を止める。nullを原因なしと解釈しない。
4. 必要なら最大2回の明示retry、ホームからの続き、完全終了後のcold再開を確認する。retryは確定進行を保ち安全なcheckpoint位置へ戻る。失敗して未提示のposeをそのまま保存位置として復元するものではない。retry上限後も詳細を読め、ホームでもこの起動の直近3試行を参照できる。コピー不能時は本文を選択する。外部への自動送信はない。

記録欄: `日付 / device / iOS / app version / 新native build / EAS build ID / code・profile / 04入場由来 / 初回提示 / 鍵・レバー・鏡・巻上・05 / retry・home・cold / FIRST_FAILURE（発生時） / 未確認`。04→05が確認できた後で全01→05とオフライン候補を評価する。表示、実指、VoiceOver、錯視、怖さ、実聴、FPS／発熱は端末で実施した項目だけ記入する。ローカル検査をiPhone合格へ置き換えない。


## Goal 013.1 r7 — 診断の残り時間・識別情報を確認する（実機未確認）

最新のローカル識別子は`goal-013-1-mirror-runtime-r7`。r6のpreview診断導線を保持し、起動残り時間の誤記録、待機中コピーのbuild情報、schema番号を修正した。新しい端末結果はなく、停止原因の特定と実機受入れは引き続き未完了。`DEVICE_ACCEPTANCE=PENDING`、`RELEASE_READY=false`。

1. ユーザーが次のworktreeからpreviewを作り、その**完成したpreviewのインストールリンク**で既存アプリを更新する。EAS build IDとiOS build番号を記録する。アプリやセーブを削除しない。

   ```sh
   cd /home/mhirotaka/workspace/chroma-rift-goal012
   git status --short --branch
   git log -1 --oneline
   eas build --platform ios --profile preview
   ```

2. iPhone上でアプリを直接起動し、ホームの`goal-013-1-mirror-runtime-r7 / preview / iOS build <番号>`を確認する。異なる場合は再検証結果をr7へ結び付けない。previewは同梱ReleaseなのでMetro/トンネル/開発クライアントのQRでは更新されない。r7では依存・native設定を変更していないが、インストール済みバイナリの内容は未確認。
3. 既存の「続きから」で04へ入り、キーの有無→練習保持→鏡の視界内外→本作業→05を確認する。準備中にも「描画の診断」からversion/build/schema付き本文を表示・コピーできる。失敗したら「詳細を表示」→「診断情報をコピー」。Clipboard失敗時は選択可能な本文を長押しする。retry上限後も詳細があり、ホームの「直前の描画診断を表示」で直近3試行を読める。
4. 再試行では確定済み進行を保って認可された安全な再開位置へ戻る場合がある。04でホーム／完全終了から再開し、キー・歯止め・現在エリアを確認する。04→05が確認できてから01→05の通しとオフラインRelease候補へ進む。停止時は最初のreason/stack、startup/readiness、最後の主描画・反射・提示番号、code/profile/native buildを保存する。診断は自動送信されない。

このローカル作業でクラウドビルド・インストールは実行していない。表示・タッチ・VoiceOver・実音・FPS／発熱は実機で確認した項目だけ記入する。以下はr6以前の履歴。

## Goal 013.1 r6 — 再発時に最初の故障を取得する（実機未確認）

2026-09-15の新しい動画では、エリア04が表示された約12秒後に再びアプリ内エラーへ移った。EAS preview build ID `89bd24f7-0be9-4fd7-82c8-414b0937813f` の完了は分かるが、そのバイナリを録画端末が使ったか、r5が入っていたかは不明。現状のr5を同条件で再ビルドしたり、最初から全章を遊び直したりするより先に、r6診断入りpreviewでバイナリを識別する。`DEVICE_ACCEPTANCE=PENDING`、`RELEASE_READY=false`。コード識別子がr5だったと確認できた録画ならr5の実機判定をFAILへ更新する。

ローカルでの変更はまだビルド・配布していない。作業木 `/home/mhirotaka/workspace/chroma-rift-goal012` のcommitと差分を確認し、ユーザーが `eas build --platform ios --profile preview` を実行する。previewには`autoIncrement`を設定したが、remoteの次のbuild番号はここでは未確認。EASの新しいbuild IDとiOS build番号を別々に記録し、完成した**previewのインストールリンク**をiPhoneで開いて既存アプリへインストールする。端末上でアプリを直接起動し、第一章ホームに `goal-013-1-mirror-runtime-r6 / preview / iOS build <番号>` が出ることを先に確認する。出なければr6の実機検証を始めず、インストール元と実行中アプリを照合する。Metro/開発クライアントのQR、8081/8082のトンネルは同梱previewのJSを更新しない。アプリや保存データは削除しない。

1. 第一章ホームの「続きから」で既存エリア04へ入り、鍵が保存済みか、未取得かを確認する。練習レバーを実指で保持し、鏡を正面・視界外・背面・再び正面で見て、巻き上げて04出口から05へ進む。
2. エラーならその画面上部のr6/profile/iOS buildを撮り、「詳細を表示」→「診断情報をコピー」を押して`FIRST_FAILURE`を保管する。Clipboardが失敗したら表示された本文を長押しして選択する。retry上限後も詳細を開ける。ホームへ戻った後は「直前の描画診断を表示」からこの起動の直近3試行を読める。診断は自動送信されない。
3. 04でホームへ戻る／完全終了から再開し、鍵と確定済み歯止め、エリアを確認する。04→05が安定した後に01→05の通しとオフラインRelease候補を確認する。端末機種、iOS版、新build ID/native build、表示されたcode/profile、04入場由来、キー・練習・鏡・巻上・05、retry/home/cold、最初のreason code/stackを記録する。実測していないfps・実聴・表示成功は書かない。

以下のr5手順と結果は履歴として残す。r6の端末合格を示すものではない。

## Goal 013.1 — エリア04の描画失敗を再確認（未実施）

2026-09-14の端末録画では、04の鍵・練習レバー付近で一度3Dが表示された後、約08:26にアプリ内エラーとなり、再試行とホームからの続きも録画内では復帰を確認できなかった。修正コードの識別子は `goal-013-1-mirror-runtime-r5`。現在のローカル検査はiPhoneの合格判定ではなく、`DEVICE_ACCEPTANCE=PENDING` のまま。

1. 端末のアプリ種別（Development Build／preview／TestFlight）、端末・iOS、build番号、実際に読み込んだコード識別子を記録する。Development Buildは `/home/mhirotaka/workspace/chroma-rift-goal012` で `npx expo start --dev-client --tunnel --clear` を起動してJS更新を読み込める。既存Metroが動いていれば先に停止する。preview/TestFlightの同梱Releaseにはこの作業木のJSが自動で入らないので、修正コードを含む新しいpreview等を用意してから判定する。今回のローカル作業ではEAS build、配布、署名を行っていない。
2. アプリや保存を消さず、既存の「エリア04から続き」で入る。鍵の有無と目的文を確認し、鍵取得前なら取得する。淡い壁印をたどって練習レバーを実指で保持し、鏡を視界に入れる／外す／背を向ける／再び入れる。古い像の残留、暗転、エラーを記録する。
3. 鏡を見ながら巻き上げ、格子を越えて04出口を実指で使い、05の制御室まで進む。04で一度ホームへ戻り「続きから」を試し、完全終了後のcold再開でも鍵・確定済み歯止めと現在エリアが保たれるか確認する。失敗した場合はエラー画面の「詳細を表示」→「診断情報をコピー」を自分で選び、`FIRST_FAILURE` のreason code、最初のstack、最後の主描画／反射／提示frame、失敗時poseと`poseSource`、FBO/GL status、build情報を保存する。診断は自動送信されない。
4. 04の再発がなければ、01→05の通常経路、05の本来の終了条件、オフラインRelease候補で同じ箇所を確認する。録画あり／なし、怖さ設定、pause／背景／復帰も区別する。画面の見え方・実指・VoiceOver・実音・fps／発熱は実測した項目だけ記録する。

記録欄: `日付 / tester / device / iOS / build番号 / channel / code識別子 / 04入場由来 / 鍵・練習・鏡・巻上・05 / retry・home・cold結果 / FIRST_FAILURE（発生時） / 録画有無 / 未確認`。この結果を得るまで「実機修正済み」としない。

**以下は実施待ち。** Goal 013はexpo 57.0.22とnative Expoモジュールの推奨パッチ、1.0.0、preview/production設定を変更した。従来のDevelopment BuildやMetroでのJS更新を、新しい配布バイナリの確認と扱わない。EAS Build、Apple認証、TestFlightアップロードはこのローカル作業では行っていない。[公開ゲート](RELEASE-CHAPTER-1.md)を先に読む。

保存失敗の端末確認では、通常checkpointと物語提示で再試行/起動中のみの選択肢、危険停止、実保存と画面表示の一致を確認する。端末のデータを意図的に壊さず、再現可能な条件でのみ行う。

[JestのApp経路ログ](qa-goal013/app-natural-route-standard-cold.json) には、4回のエリア遷移後cold restore、10回の再入場、05→01逆順replayを記録した。これはnative Canvas・実AsyncStorage・音ownerを使わないため、以下の端末手順はすべて未実施のまま。

1. 端末の既存CHROMA RIFTを消さず、旧セーブのキー・章・設定・発見を記録する。同じ `com.hirotakam.chromarift` のpreviewで上書きした後、旧原文を保持した移行候補と継続を確認する。遠隔EAS buildNumberと配布済み番号を照合してからビルドする。
2. Metroを停止し、previewまたはTestFlightの**端末バイナリ**を機内モードで新規起動する。初回の仮面、ハイブリッド画像、音、04の鏡、05の表示がネットワーク待ちで欠けないことを確認する。端末モデル、iOS版、build番号、commit、文字サイズ、明るさ、音量、怖さを記録する。
3. 第一章を新規に開始し、01非常回路→02管理経路→03防火幕→04隔離キーと鏡越しの巻き上げ→05誘導・全身収容・隔離・停止・職員出口・屋外まで、同じrunIdで通す。04の二つの横顔と中央の鍵を最初に確認し、取得時に鍵が浮いて消え、04の巻上機へ差すと保持中だけ機械に現れ、指を離すと戻ることを確認する。05では同じ輪郭の鍵が設置前には無く、差し込むと制御盤に残ることを確認する。鍵取得後・設置後のcold再入場でも表示と保存状態が一致するか記録する。05の観察窓から巡回体の頭から足、収容内の床灯、閉じる扉を確認し、頭上の受鈴器が身体を隠さないか記録する。01〜04で未読の点検記録がエリア境界に出る場合、旧Canvasが止まり、読み終わるまで次のCanvasが重ならないか確かめる。途中の物語文は実際の操作可能/一時停止状態で読め、閉じた直後に古い指入力や音が再生されないか確認する。
   04で格子を越えた先に制御室前室の床・壁・扉・灯りが見え、黒背景や浮いた四角い出口マーカーがないことを記録する。05で職員出口を越える際も空・遠景・歩道・灯具が欠けず見えることを確認し、両出口操作が実指で受理されるか記録する。
   01→02、02→03、03→04の各館内境界でも、最後の扉を越えた先に次区画の扉面・枠・小灯が見え、無地の壁だけで途切れないか記録する。
4. 04作業中の離脱/捕捉/再開、05の誤閉鎖/復旧、各境界のアプリ強制終了/コールド再開を行う。点検記録の表示中にも終了し、次エリアの安全入口で未読文だけが再表示されることを確認する。01〜04で全体完了や屋外が出ず、05の停止後は在館01、屋外後は00が保存されることを確認する。
   屋外では職員出口から歩道を実際に歩き、完了操作の前後で視点が先へ飛ばないか確認する。`在館反応 00` と閉館完了文、外の景色、第一章の結末、終了・再起動後の完了記録を別々に確認し、どの画面で観察したか記す。ローカルの05動画にある完了字幕はQA合成で、製品HUDの証拠ではない。

5. 標準/控えめの両方、無音、VoiceOver、左右手、ドラッグ/ボタン操作、fontScale 1〜2、320〜430pt相当の表示とsafe areaを試す。最小幅・最大文字で03の灯り装置を開き、説明の下部までスクロールして「灯りを固定して扉を開く」と「観察を終える」へ実指とVoiceOverで到達できるか確認する。04・05の一時停止から発見メモを開き、未観察項目が増えず、04の図地反転と鏡の説明が別に読めること、閉じても危険中の入力が漏れないことを確認する。両エリアで怖さを変更しても所持品・歯止め・隔離進行が戻らないか確かめる。操作不能な機器、読み物中の追尾、HUDとの重なり、出口の触れにくさを記録する。錯視・怖さ・実聴は自由記述し、自動検査から知覚を推定しない。
   第一章ホームも初回/旧記録引き継ぎ、進行中、完了、5エリアの振り返り、発見記録を最大文字で開く。「01」〜「05」の番号が1行で読めること、全ボタンをスクロールして実指/VoiceOverで選べること、第二章が予定だけの案内であることを確認する。先行のbrowser CSS記録は `qa-goal013/home-layout-report.json`。
   01→02、02→03、04→05の点検記録で本文と「点検を続ける」が最大文字でも見切れず、VoiceOverで順に読んで閉じられるか確認する。結末は未提示の収容手順が追加される場合も含め、最下部までスクロールして「エリアを振り返る」「発見の記録」「ホームへ戻る」の3つへ実指とVoiceOverで到達できるか記録する。browser CSSでの先行画像と寸法は `qa-goal013/ending-layout-report.json` にあり、native Yogaの改行・safe area・読み上げ順序はここで別に判定する。
   02の留め金説明、04巻上機の保持前/中/指離し、05の隔離キー・手順・ベルでは、最大文字でも中段の対象名が左スティックを覆わず、目的・pause・操作ボタンが読めて実指で使えるか確認する。02の長さ/鉛直装置で補助を開き、パネルを途中までスクロールして「探索へ戻る」も実指とVoiceOverで選べるか試す。03の映写機を作動させ、開始通知が目的欄に一時表示された後、目的文に戻ること、取得不能理由と映写機状態を同時に積まずに読めることも確認する。01初期画面も同じ条件で確認する。ローカルのCSS近似画像は `qa-goal013/stage-hud-report.json`、`qa-goal013/vault-large-text-report.json`、`qa-goal013/theatre-projector-hud-320-audit.json` にあり、端末のYoga・safe area・実sceneとの重なりをここで別に記録する。
6. 同じ設定で10〜15分連続プレイと10回以上の入退場を試し、各エリアのframe time、draw calls、triangles、鏡RT寸法、メモリ/音owner、端末の熱さを記録する。録画ありとなし、開発版とpreviewを分ける。

実施結果の書式: `日付 / tester / device / iOS / build番号 / commit / 設定 / 手順 / 観察 / 動画またはログ / 未確認`。合否は実際のiPhone観察後に記入する。以下の旧Goal010以降は当時の独立ステージ手順の履歴であり、現在の第一章ホームや完了条件ではない。

# Goal 010 — iPhone でステージ選択と「影の映写室」を確認する

**この節の実機確認はすべて未実施。** 実 domain / controller / React の検査、Three のブラウザー描画、ソフトウェア WebGL 動画、CSS で近似した HUD は、それぞれの検査範囲を示す。iPhone / EXGL / Skia / Yoga の表示、実指、知覚、怖さ、実聴、FPS・発熱を確認した証拠とはしない。

## 更新とステージ選択

1. expo-gl と expo-audio を含む既存 Development Build が今回の想定実行環境。保護 6 ファイルの最終 hash と native 変更の有無は [Goal 010 の記録](GOAL-010.md) を確認する。端末に入っている binary 自体は遠隔確認していない。アプリ削除、保存の全消去、EAS Build、認証をこの確認の前提にしない。
2. 既存 Metro が動いていれば停止したうえで、WSL の作業場所から次を実行する。接続・tunnel の失敗とゲームの描画失敗を区別する。

       cd /home/mhirotaka/workspace/chroma-rift
       npx expo start --dev-client --tunnel --clear

3. CHROMA RIFT の 01「閉館後の展示室」、02「測れない収蔵庫」、03「影の映写室」と、別枠の「帰り道のない入口」が読めることを確認する。今回の未開始／探索中／脱出済みと、過去の脱出・発見は別の表示。
4. 各カードを選ぶだけで入場履歴が変わらず、準備から実際に部屋へ入った後だけ「続きから」がその章を指すことを確認する。複数の旧セーブしかない初回は入場順を推測しない。未対応 Build、準備失敗、未知版の保存を保持して試す場合は、履歴で元セーブを上書きしない。
5. gallery 結果→vault→theatre へ進み、既存の 3 問／詳細調整を再強制しないことを確認する。既クリアの章では再プレイの確認をキャンセルして保存が残ること、確認後も準備から入るまで今回の進行が置換されないことを確認する。「クリア記録を見る」は再プレイではない。
6. 入場前の設定で怖さ・音・見え方・左右手・ドラッグ／簡単操作・感度を変更し、準備へ戻る。別の章でも共有され、文字拡大や Reduce Motion だけで保存済みのドラッグ希望が変わらないことを確認する。

model / iOS / Build / 診断 revision **goal-010-theatre-r1** / 普段の明るさ / True Tone / Night Shift / 論理画面幅 / fontScale / 音と怖さを記録する。開発用 Downloading 表示と、実フレーム提示後の ready を区別する。

## 既存装置への入り方

- vault の長さ装置を通常の観察域から見る。近すぎる、遠い、左右・上下が欠ける、壁や棚が間にある状態を試し、操作ボタンと短い修正案内が同じ現在の見え方に対応するか確認する。
- 320×568 / 390×844 / 430×932pt 相当、fontScale 1〜2 で、板と取っ手が全部見え、上の目的文・下の状態やボタンと重ならないか確認する。スクロールで戻る操作まで届くことも確認する。CSS 近似で合格した寸法でも、native Yoga の再確認が必要。
- 最初の「歩く」「見回す」の案内が、実際の移動と視点操作をした後には残り続けず、一時停止の操作説明は読めることを確認する。新しい棚端・高い枠・床の区切りを区別できたかも記録する。

## 影の映写室を通る

1. まず大きな投影の影と、取っ手のある灯りを確認する。保護された作業場所で灯りをドラッグし、実物の小さな物・影・左右の受光窓の関係を比べる。窓は明暗だけでなく形や意味ラベルでも区別できるか記録する。
2. 光を外す誤った位置、指の離し直し、外 drop、キャンセルを試す。最後に窓へ光が届いた位置で指を離し、別の「灯りを固定する」を押す。光源や正解がカメラ、音、色、品質、怖さで変わらず、比較や一定時間待つだけで開かないことを確認する。
3. 任意の部屋を正面→途中→横から見る。シャッターを開け、平らな観察通路を歩く。置物や部屋が大きさを変えたように**感じたかどうかは自由記述**とし、期待する答えを先に伝えない。同じ実寸の置物・固定した歪んだ壁と床であり、視点移動時の差替えや FOV 演出がないことを別に確認する。
4. 観察後の保守通路を使う周回と、部屋を飛ばして主通路を進む周回を試す。部屋の主観的な大小回答を要求せず、任意経路が灯りの必須条件を飛ばさないことを確認する。
5. 動く展示体と、投影の元の静物、部屋の二つの置物を区別する。足・頭・身体の動作、見通し、遮蔽、予告→接近→捜索、捕捉後の安全地点への復帰を確認する。同じ起動中の pause だけで敵の記憶や位置が都合よく初期化されないことも確認する。
6. 映写機を使わない場合と使う場合を、同じ初期状態から比べる。取っ手を回すと有限の運転と短い再使用待ちがある。**映写機の操作中は世界が止まらない。** 実際に音源を聞けた敵が調べ、プレイヤーを現に見ている敵が必ず目をそらす仕組みにはなっていないかを確認する。
7. 保護された操作側で、目に見える取っ手を向いて「防火幕を下ろす」。受理、目に見える降下、閉じた遮蔽、その先を自分で歩く脱出を区別する。幕を見た時や下ろし始めただけで結果にならず、閉じた幕越しに捕捉されないことを確認する。
8. 幕の途中で background／復帰して受理済みの進行が残ること、アプリを終了してコールド再開したときは安全な閉じた幕へ復元し、未通過なら未クリアのままで出口へ歩けることを確認する。結果は持ち越した指の touch-up で閉じず、新しい押下で動くことを確認する。

初見 6〜10 分は設計目標で、実測時間でも制限時間でもない。照明を極端に上げる、片目を隠す、長く凝視する、ヘッドフォンを使うことを前提にしない。不快な場合は pause・退出する。

## 発見メモ・保存・中断

- 新章のメモは「影の大きさ」「部屋の奥行き」の二件だけ。本編で未観察の項目がネタバレとして並ばず、観察と単なる通路開通を区別する。クリア後の自由比較では敵が動かず、未体験を発見済みにしない。
- 影の灯り slider と受光面／上面構造図、部屋の正面／途中／横の比較を操作する。同じ本編 Canvas の所有者を保ち、本編のカメラ・進行・敵が変わらないことを確認する。構造図を使った比較を、自然な見え方の知覚実績にはしない。
- 光のドラッグ、映写機の途中、敵の接近、メモ、幕の降下で pause／背景／復帰を試す。古い指や音の callback が復活せず、全指を離した新しい操作が必要なことを確認する。
- 章を順に再開し、旧原文／backup、各章の進行、共有設定が残ることを確認する。単章再プレイはその今回の進行だけを置換し、過去の脱出・発見を維持する。保存失敗の案内と、この起動中の同じ seed・進行の維持を確認する。未知 raw の保護確認のために全データ reset を要求しない。

## 実機でまだ確認していない受け入れ項目

| 未確認 | 記録する内容 |
| --- | --- |
| [ ] ステージ一覧／入場 | 狭幅・大文字・safe area・縦スクロール、最後の実入場、次章、履歴と今回の状態、キャンセル・replay・記録閲覧 |
| [ ] 実指と44pt | 二指の左右独立、片指終了、画面外保持、第三指 pause、取っ手・別確定・メモ・幕・結果、全指解放後の再操作 |
| [ ] VoiceOver／設定 | 同じ rail の調整と窓のラベル、別固定、映写機の部分回転、幕、メモ・出典・戻る。左右手・感度・Reduce Motion・文字拡大で保存した操作希望が残る |
| [ ] 光と空間の知覚 | 大きな影と小さな物の解釈、正面／横の部屋と置物の見え方。効果なし・不明もそのまま記録。構造図や補助ありの条件を分離 |
| [ ] 恐怖／対処 | 原因の見える敵、予告、音源への調査、実視認の優先、遮蔽と別通路、捕捉後の復帰、標準／控えめ。単に敵が存在することを怖さの確認にしない |
| [ ] 無音と実聴 | 消音でも映写機の世界内 noise と成功条件は同じ。実際の運転音・足音・幕の一撃・停止・音量を実聴し、backgroundや中断時の残音を確認 |
| [ ] 10回の実入退場 | ホーム、章切替、メモ、背景、描画retryを含め、GL context / renderer / geometry / texture / audio player / memory が増え続けないか |
| [ ] 実機性能／温度 | 同じ地点・経路で開発／録画／本番相当を分け、frame-time 分布・fps・draw calls・triangles・memory・温度を記録。動画の録画fpsやソフトウェアWebGL速度をiPhoneの実性能にしない |

[新章の説明と攻略](SHADOW-THEATRE.md)、[検証記録](GOAL-010.md)、[描画・動画 QA](qa-goal010/README.md)、[出典台帳](ILLUSION-SOURCES.md) を参照。以下は旧版時点の履歴であり、現在のホーム文言や新章条件へ読み替えない。

## Goal 009 — iPhone で「測れない収蔵庫」を確認する（履歴）

**以下の新章手順は実機未実施。** 自動検査、実 Three mesh のブラウザー WebGL、native 境界を代替した React テストを、iPhone / EXGL の表示、実指、知覚、怖さ、実聴、FPS の確認と混同しない。

## 旧記録を消さずに新章へ入る

1. expo-gl と expo-audio を含む既存 Development Build で新版 JavaScript を読む。最終 diff で依存・native の保護 6 ファイルが不変なら再ビルドは不要。アプリ削除、全データ reset、EAS Build、認証はこの手順の前提にしない。
2. ホームの「測れない収蔵庫を始める」を選ぶ。保存済みなら「収蔵庫の続きから」。前章の結果にも「次の章へ：測れない収蔵庫」がある。前章未クリアでも直接試せ、そのクリア状態を書き換えない。
3. 調整済みなら初回 3 問が再び出ないこと、音・怖さ・操作希望が共有されることを確認する。新規一周を試すときだけ「この収蔵庫を最初から」を選ぶ。
4. 診断 revision **goal-009-uncanny-r1** と、新しい棚・留め金・針の実表示を確認する。開発用 Downloading とアプリの ready 待ちを区別し、ロード文言だけで描画完了にしない。

接続は従来の npx expo start --dev-client --tunnel --clear を使う。Metro が既に動いていれば二重起動しない。Build に音 module がない場合は更新案内を記録し、無音の継続可否を分ける。

## 最初の操作と 20〜30 秒の敵動作

- iPhone model / iOS / Build / revision / 明るさ / True Tone / Night Shift / logical width / fontScale / 音設定 / 怖さを記録する。
- 入場して搬出通路を開ける目的と右端の取っ手が分かるかを記録する。最初の約 20 秒・初見 6〜10 分は設計目標で、時間制限や実測ではない。
- 留め金で、下の棒を直接ドラッグ→指を離す→「固定する」。飾りあり・なし、測定ガイドあり・なしで、同じ端点・太さ・カメラを比較する。効果の有無は誘導せず自由記述する。
- 格子の奥や棚の通路で 20〜30 秒ほど、停止、頭の先行、胸・骨盤、踏み替え、歩き出し、停止を観察する。90〜180°の転回が一括切替に見えないか、巡回と発見後でシルエットが違うかを記録する。
- 金属床の速歩と遅歩、棚の左右で視線を切る行動、最後の位置の捜索を確認する。標準では予告を見て横へ退避できるか、控えめでは同じ章を追尾・捕捉なしで終えられるかを別々に記録する。
- 制動ベイで針を直接回す。傾いた枠を消す・下げ振りを出す比較が、針や目標を回していないことを確認する。上下が逆でも同じ鉛直の棒として扱う。指を離してから「ロックする」。
- カフェウォールは小さな任意の壁だけ。明暗の比較で目地や地形が曲がらず、未観察でも出口へ進める。

## 終了・保存・旧 UX

- 棚または閉められる仕切りで敵の視線を切り、搬出口の内側へ入る。扉面と取っ手を自分で見ると閉扉が出て、壁へ向くと出ない。小さな点への厳密な照準や強制振り向きを要求しない。
- 「扉を閉める」後は降下→向こう側の一撃→静けさ→結果が伝わるか記録する。約 1.4 秒の余韻を保存待ちにせず、背景・失敗・再開でもクリアが戻らない。直前の指を離しても結果ボタンが動かず、新しい押下で動くかを確認する。
- 同じ起動中の pause / resume で敵の記憶が消えて毎回安全になる挙動がないか確認する。ホームからのコールド再開は最後に実際に訪れた安全地点へ戻り、即攻撃を受けない。
- 新章の発見メモ三項目を操作し、補助の現在状態と出典が読めるか確認する。結果の自由比較は未観察の discovery を増やさない。
- 既存展示室の仮面・掲示は観察後の短い説明と「構造をメモで比べる」から対応する比較を開けるか確認する。本編の objective / pose / 同じ Canvas を維持する。B/C/配線のメモでも補助状態を明示し、自然な知覚の証拠とされない。
- 旧章・展示室・新章の「続き」を切り替え、それぞれの解放・クリアが保たれることを確認する。新章だけの reset で共有設定・旧原文と backup が消えないことを確認する。

## 未確認の受け入れ項目

| 未確認 | 記録 |
| --- | --- |
| [ ] 実指と画面幅 | 320×568 / 390×844 / 430×932pt 相当、fontScale 1〜2、safe area、48pt ボタン・44pt 以上の意味操作。板全体・取っ手・上の目的・下の短い状態と scroll が重ならない |
| [ ] 所有権 | 二指の片方だけ終了、画面外へ保持、外 drop rollback、第三指 pause、復帰の全指解放、古い callback。文字拡大 / Reduce Motion だけで drag 希望を奪わない |
| [ ] 読み上げ | VoiceOver の長さ 2cm / 針 2°、補助、別の固定・ロック、探索、メモ出典、閉じるまで届く |
| [ ] 知覚 | ミュラー＝リヤー / Rod-and-Frame / Café Wall を文脈あり・なしで自由記述。ガイドを使った試行を自然な錯視体験と記録しない |
| [ ] 恐怖と対処 | 頭→身体→足、背後速歩の反応、LOS、遮蔽→捜索、予告→回避、格子の安全、身体の壁抜け、標準と控えめ |
| [ ] 無音 / 実聴 | 無音でも AI と成功条件が同じ。実足の接地と一回の足音、金属、布、扉の原因とタイミング、音量、停止。方向音や医学的安全を未実装のまま主張しない |
| [ ] 10 回の実再入場 | ホーム、pause、背景、メモ、描画再試行で renderer / context / image / 音 player / memory が増え続けないか |
| [ ] 実機性能 | 開発・録画中・本番相当の frame time / fps / 発熱 / memory。録画 60fps、テスト数、draw call 数をゲームの 60fps や怖さの証明にしない |

[攻略とヒント](UNCANNY-VAULT.md)、[出典台帳](ILLUSION-SOURCES.md)。以下は Goal 008 以前の履歴手順で、新章の進行条件や現在の診断 revision と読み替えない。

## Goal 008 — iPhone で知覚と怖さを確認する（履歴）

**以下は実機未実施の手順。** コード・native 境界・ブラウザーの描画検査を、実機表示、実指、知覚、怖さ、実聴、性能の確認結果に置き換えない。依存 / native 設定を変えず、expo-gl と expo-audio を含む既存 Development Build が新版 JS と同梱素材を Metro から読み込む。今回のための EAS Build、認証、アプリ削除、全データ reset は行わない。

```sh
cd /home/mhirotaka/workspace/chroma-rift
npx expo start --dev-client --tunnel --clear
```

既存の同ポートの Metro が動いている場合は先に終了する。接続後は表示された壁・床・装置と「描画の診断」からコピーした revision を記録し、revision `goal-008-perceptual-r1` を確認し、ロード文言だけで表示済みとしない。録画や診断に URL / QR / account 識別子を含めない。

## 最初の 5 分

1. iPhone model / iOS / Build 版 / revision / 普段の明るさ / True Tone / Night Shift / fontScale / 消音と音量を記録する。初回の 3 問はスキップできる。ホーム「新しい展示室を始める」、または旧クリア結果から章だけ再プレイを選ぶ。
2. 非常灯と出口盤を調べ、色の展示を普段の条件で見て任意に比べる。前後色の期待を教えず、分かりにくい場合も進める。
3. 固定した凹面仮面を正面と横から見る。窓を開閉し、仮面自体の回転や差替えがないか確認する。ハイブリッド掲示へ近づき離れ、どの図柄が読めたか自由記述する。同じ画像かを確認し、錯視を感じないことを不合格にしない。
4. B または C を操作する。B は 0→1→2 枚の案内、同じ灰色の中心、独立した「比べる」。C は円盤の縁、0..3/3 と独立した引き出し操作。通常中央に線・面・照準がなく、補助だけ明示されることを確認する。電源を取った後の予兆と横切りを見たか、気付かなかった場合はそのまま記録する。
5. pause→発見メモ。発見した項目のみ出て、それぞれ実際に比較を操作できるか確認する。仮面の比較視点を横へ動かして閉じ、本編の位置と向き・展示体が進まず戻ることを確認する。音は任意で、いつでも stop。音 off / 演出音 off / 控えめでは再生しない。

5 分は観察の区切りで、攻略の時間制限・実測所要時間ではない。強い照明、長い凝視、片目を隠すこと、ヘッドフォンを要求しない。不快なら pause・退出できる。

## 最後までの確認

- B→C と C→B をそれぞれ解き、別操作で二個を取り、出口盤で電源を接続する。A / D は新版の必須条件・結果に出ない。
- 配線へ進み、右の取っ手で高さ、下の取っ手でカバーを横へ動かす。固定した高さで cover だけ退けても線の位置・正解・camera が変わらない。比較なしでも合わせられれば接続できる。誤答・cancel・外 drop は即死や解き直しにせず、指の解放後の「接続する」だけで開く。
- 開いた通路で予兆・姿を見て、棚の陰へ退く→待つ→抜ける。見通しと身体 / 衝突が一致し、突然の移動・不意の消失がないか記録する。控えめでは同じ経路を危険なしで進められる。
- 安全な敷居まで歩き、「扉を閉める」を押す。ドラッグ操作でも押せること、閉扉が見え、短い衝撃音の後に静まり結果へ進むことを確認する。閉扉後に捕まらず、再開してクリアが戻らない。
- 結果「発見メモを比べる」で七項目を敵なしの自由比較として開く。未体験を発見済みに書き換えない。各項目の操作・説明・出典・素材クレジットが offline で読める。

## 保存を残して新版を試す

v2 の途中なら「展示室の続きから」で B/C / 電源 / 接続を引き継ぐ。既に接続 / 終盤 / クリアした記録には配線互換開通があり、旧クリアを新版体験済みとは表示しない。新版を最初から体験する場合だけ「この展示室を最初から」または結果の再プレイを選ぶ。v1 / v2 原文・backup、旧章、共有設定は残る。全データ reset は移行確認の手順に使わない。

## 未確認の実機項目

| 項目 | 記録する内容 |
| --- | --- |
| [ ] 実指・44pt | B / C / 配線の取っ手、cover、pause、戻る、閉扉。HUD と板の重なり、外 drop、持ち直し、全指解放、別指 HUD、3 本指 pause |
| [ ] 幅と文字 | 320〜430pt / fontScale 1〜2 / safe area / 左手 / 簡単操作 / VoiceOver。説明・比較・出典・stop・閉じるへスクロールで届くこと。文字拡大だけで drag を強制変更しない |
| [ ] 知覚 | 色 / 明暗 / 輪郭 / 凹面 / 配線 / hybrid の各々を自由記述。ブラウザー画像と別に記録 |
| [ ] 怖さ・公平さ | 予兆→初回電源後の横切り→通路追跡→棚待ち→通過→閉扉。標準 / 控えめ、接触復帰と猶予 |
| [ ] 実聴 | 足音、布、閉扉、Shepard。出始め・音量・click・停止、無音 / 控えめ / 演出音 off、background / failure / note close の停止 |
| [ ] 中断と保存 | 配線 drag / メモ / 巡回 / 閉扉で pause・background・描画再試行。旧 pointer・過去の字幕や音を再生しない。v2→v3 / v1→v3・未知版保護・章だけ再開始 |
| [ ] 10 回 | メモ開閉 / 仮面比較 / 音再生停止 / ホーム再入場 / background 復帰。renderer / context / texture / audio player と memory が増え続けないか実測 |
| [ ] 性能 | fps / frame time / 発熱 / memory。通常開発・録画・本番相当を分ける。60fps と錯視 / 怖さの強さを自動検査から推定しない |

[新版設計](PERCEPTION-GALLERY.md)、[出典台帳](ILLUSION-SOURCES.md)、[素材 QA](qa-goal008/materials/README.md) を参照。以下は過去版当時の手順であり、Goal 008 の終了条件や native Build 要件として読み替えない。

## Goal 007 — iPhoneで「閉館後の展示室」を確認する（履歴）

この節は新版 `levelVersion=2` の手動確認手順で、**実機では未実施**。Jest の native event 検査、実 domain / controller の完走、ブラウザーの描画 QA を、iPhone の表示・指操作・知覚・怖さ・音・性能の確認済み結果に置き換えない。

Goal 007 では依存関係と native 設定を変更していない。**expo-gl と expo-audio を含む既存 Development Build** で Metro の新版 JavaScript を読み込む。Goal 007 のために EAS Build、認証、アプリの削除、全データ reset を要求しない。expo-audio のないさらに古い Build に出る音 module 更新案内は別の条件で、無音でもゲームは続けられる。

```sh
cd /home/mhirotaka/workspace/chroma-rift
npx expo start --dev-client --tunnel --clear
```

既存 Metro が同じポートで動いている場合は、その実行を終了してから起動する。接続できたらホームの「閉館後の展示室」、最初の「非常灯を点ける」、コピーした診断 revision が `goal-007-horror-r1` であることを記録する。ロード中表示だけでなく、壁・床・スイッチ・展示体の実形状を確認する。

## 記録を残して新版を試す

- 初めてなら「新しい展示室を始める」。初回 3 問はスキップでき、怖さは入場前か一時停止の設定から選べる。
- v1 の途中セーブがある場合は、まず「展示室の続きから」で B/C 解決・取得・接続の引き継ぎと安全な再開位置を確認する。旧座標のまま再開することを期待しない。
- v1 のクリア済みなら結果の「展示室のクリア記録」を確認する。その記録は新版を体験した証明ではない。新版を最初から試す時だけ結果の再プレイ、または一時停止の「この展示室を最初から」を選ぶ。設定では対象名が「閉館後の展示室だけを最初から」であることを確認する。
- 章だけの再開始は新版 v2 を作り直し、旧 v1 原文・移行 backup、旧章、共有設定・調整を残す。**「保存データをリセット」はこの確認手順に使わない。** 旧「帰り道のない入口」は「旧章の続きから」または「旧章を遊ぶ」で独立して確認する。

## 新版の短い通し手順

1. model / iOS / Build版 /診断 revision /明るさ / True Tone / Night Shift /文字サイズ /消音設定を記録する。標準か控えめかを記録し、途中で変更した箇所も残す。
2. 初期目的「出口を探す」と大きな非常灯スイッチを見て、説明を追加せず最初に何をすると思ったかを記録する。「非常灯を点ける」の操作とレバー・導線灯・格子越しの展示体を確認する。初見 10〜20 秒は観察目安であり制限時間にしない。
3. 出口盤を調べて「予備電源を探す 0/2」。B/C の左右の道へ進めること、旧紋章 A、旧投影鍵 D、広い入口への差替えが新版の必須条件や結果に出ないことを確認する。
4. B で目的「同じ灰色の2枚を、下の四角い枠へ置く」を読む。四角い見本のつまみから直接動かし、0 枚→1 枚→2 枚で案内が変わり、2 枚の別 ID と指の解放後だけ「比べる」が有効になることを確かめる。不一致メッセージは入れ替えるまで残る。中心の灰色は番号・つまみ・選択表示で覆われない。
5. B の持ち直し・枠の入替え・元位置への返却・外 drop / cancel を試す。「背景をそろえる」は任意で、元 / 中立 / drag 中 / 枠上で見本の中心色が変わらず、背景比較だけでは解決しない。「動かし方」は手順だけで正解を配置しない。解決後は別の「電源を取る」で所持数が 1/2 になる。
6. C で「3枚を回して、切れ目を中央へ向ける」と、円盤のふちを回す案内を確認する。中心横断・持ち直し・角度の折り返しで跳ばず、0/3→3/3 の表示が実際の向きと対応する。3/3 かつ指を離した後だけ「引き出しを開く」が有効。通常は中央の三角形を描かず、任意の輪郭ガイドだけが破線と使用中表示になる。解決後、別操作で二個目を取る。
7. C→B の逆順も試す。解決と取得を混同せず、二個未取得で出口は接続できないこと、二個を出口盤で「電源を接続」すると一度だけ通路が開くこと、HUD に二個と接続済みが残ることを確認する。
8. 標準で通路へ入り、警告・姿を見て、**棚の陰へ退く→待つ→展示体の通過後に抜ける**。見える体と衝突・見通しが対応し、B/C へ追ってこないこと、見えている体が突然消えたり飛び移ったりしないことを記録する。接触時は通路手前へ戻り、解決・取得・接続は残る。押したままの指をすべて離すまで再移動しない。復帰直後に連続接触しない。
9. 一時停止で「控えめな怖さ」へ変え、展示体や気配を残しつつ追尾・接触による戻しがなく、同じ電源・出口条件で抜けられることを確認する。怖さ変更で音量、Reduce Motion、色、VoiceOver、保存した操作希望が変わらないことも確認する。
10. 最後の非常扉を明示操作し、その外へ自分で歩いて完了する。電源だけで自動クリアしない。結果に旧 A/D を攻略した記述が出ない。
11. B/C 操作中の pause・ホーム・background・描画再試行、通路中の pause・background・再開を行う。actor と音が止まり、古い指操作やイベント音・字幕をまとめて再生しない。GL failure 時の手動再試行は最大 2 回。記録した場合は「描画の診断」→「診断をコピー」で revision と状態を添える。
12. 途中でホームから継続し、B 配置 / C 確定角 / 電源 / 接続 / 一度済んだ物語が保たれることを確認する。drag 中の未確定値や追尾の瞬間状態は保存せず、安全な checkpoint から戻る。旧セーブのクリア状態を下げない。

## 実機で残っている確認

すべて未確認。実施後は環境、手順、観察、問題の有無を別記し、知覚の個人差を成功失敗に変換しない。

| 未確認項目 | 実機で記録すること |
| --- | --- |
| [ ] 最初の理解と課題説明 | 初見で押せるスイッチを認識した時間、B の 0/1/2 枚・C の 0..3/3 と明示操作の理解 |
| [ ] 実指と 44pt 以上の接触領域 | B つまみ / C 円盤縁 / 取得・戻る・pause、板と HUD の重なり、両指→片指離し、領域外 drag、3 本指 pause、接触復旧で全指解放 |
| [ ] 幅・文字・safe area | 320〜430pt 相当、fontScale 1〜2、左手、簡単操作、VoiceOver。説明 / 操作 / 取得 / 戻るへスクロール到達でき、文字拡大だけで drag 希望を変えない |
| [ ] 色の知覚と錯視 | 普段の表示環境で、序盤の色比較、B の明暗対比、C の主観的輪郭を各々自由記述。期待する前後色を教えず、強い照明や長い凝視を要求しない |
| [ ] 怖さと公平さ | 標準の予兆→不在→通路巡回→棚待ち→通過、見通しと身体の一致、接触復旧の猶予、控えめの追尾 / 戻し無効 |
| [ ] 実聴 | プレイヤーの実移動音と actor の実移動音、壁に押すだけでは歩行音が出ない、距離感、消音、環境 / 効果音量、pause・背景・failure・退出停止、字幕だけでも判断可能 |
| [ ] 保存と移行 | v1 A / B / C / D / cleared の進行引継ぎ、原文保持、二個の単調性、章だけ再開始、旧章の続き、未知版保護 |
| [ ] 性能と 10 回再入場 | fps / frame time / 発熱 /メモリ / renderer・GL context・texture・audio player の実測。通常開発・録画あり・本番相当を分け、10 回の再入場・背景復帰で増え続けないか |

不快な時は pause・退出し、状況を記録する。強い明るさ、ヘッドフォン、片目を隠すこと、特殊な目の状態は要求しない。録画や診断を共有する際は URL / QR / account 識別子を含めない。アプリ内 pause と Development Build の開発ランチャーを区別する。

新版の構成は [閉館後の展示室](PERCEPTION-GALLERY.md)、実施した自動・画像検査は [Goal 007](GOAL-007.md) と [Goal 007 画像 QA](qa-goal007/README.md) を参照する。以下は旧版当時の手順を履歴として残したもので、新版の完走条件や今回の native Build 要件として適用しない。

## Goal 006 — iPhoneで不確かな展示室を確認する（旧版の履歴）

以下は**実機では実施待ち**。新章はホームの「新しい展示室を始める」から入る。既存の「旧章の続きから」を消さずに残している。Goal005と違いexpo-audioを追加したため、音を含む確認にはユーザーが新しいDevelopment Buildを作成・インストールする必要がある。アプリ削除・全データresetは必須にしない。旧Buildは音なしで継続する設計だが、新native機能はReloadだけでは増えない。

新Buildをインストールした後、WSLでMetroを起動する。

```sh
cd /home/mhirotaka/workspace/chroma-rift
npx expo start --dev-client --tunnel --clear
```

既存Metroが起動中ならその端末でCtrl+C後に実行する。診断revisionは `goal-006-gallery-r1`。準備中の文言だけでなく、壁・床・装置の実形状が表示されることを確認する。

### 短い通し手順

1. model/iOS/Build版/普段の明るさ/True Tone/Night Shift/文字サイズ/消音設定を手で記録。初回3問はスキップ可能。設定に音module更新案内が出る旧Buildと、新Buildを区別する。
2. 入口の片側二本柱と欠けた上桁を確認。左で連続歩行、右で見回し、両指→片方だけ離す、領域外までdrag、third finger pauseを試す。pause後は新しく触るまで動かない。左手設定でも役割が整合する。
3. Aの床の輪へ歩き、紋章全体と三つの印が一緒に見える位置で調べる。通常表示の見え方を自由に記録し、任意に色をほどく。期待する前後色を教えず、長い凝視を求めない。輪郭が連続する印を押し、印→かんぬき→扉と衝突が一致するか確認。
4. 中央回廊からB/Cを好きな順に選ぶ。片方だけ解いた時はD扉がまだ閉じている。各翼からの近道が開き、解き直しが不要なことを確認。
5. Bの輪から装置全体を見て「動かす」。三つの見本を直接drag、途中で持ち直し、二枚を左右socketへ配置。「周囲を外す」前後で見本の中心色が変わらず、比較だけで解放されない。誤った組合せ、外drop/cancel後も見本を失わず、同じ灰色の別々の二枚を置いて「つなぐ」で開く。
6. Cで各円盤の縁をなぞって回す。中心横断、持ち直し、-π/π付近で跳ばない。通常表示の中央に線や塗りがなく、任意の「輪郭ガイド」は破線と使用中表示になる。各切欠きを内側へ向けて「封印に触れる」と引き出し/近道が開く。知覚とゲームの成功を別々に記録する。
7. B/C操作中は歩行・見回しが混ざらず、戻る/pauseが常に届く。背景復帰時や指を離さず操作モードを抜けた時に入力が再生されない。別指がHUDを押しても装置を誤操作しない。
8. Dへ入り床の輪から位置と視線を調整。離れた断片と外枠ノッチを見て重ね、鍵に明示操作。ラッチと帰路の扉が開く。色や立ち位置だけで解放されない。
9. 短い帰路から入口へ。覚えた非対称の枠は同じで、その先が変わる。見ている空間の差替えやteleportがない。最後の扉を自分で開き、歩いて外へ出て完了画面へ進む。
10. 途中保存・ホーム・続きからを試す。B配置/C確定角/解放順が維持され、drag途中値は戻る。新章をresetしても旧章の進行は残る。旧章の続きからも旧条件でクリア可能。
11. mute、Reduce Motion、320〜430pt相当幅、fontScale1〜2、safe area、左手、簡単操作、VoiceOverで進める。Bは同じ見本の代替観察→自分で配置、Cはadjustable action→独立commit。文字拡大だけでdragを強制解除しない。
12. 足音が実移動だけで鳴り、壁へ押し付けるだけでは鳴らない。操作音/解錠/環境音、master/環境/効果の音量、silent switchを確認。pause/background/描画失敗/退出で止まり、再開時に過去の音をまとめて鳴らさない。

初回は再入場・背景復帰を3回、継続性能確認では10回行う。renderer/context/texture/audio playerが増え続けないかを実測し、通常の開発実行・録画あり・本番相当を分けて記録する。60fps、実機の音量/発熱、錯視の強さ、8〜12分の初見目標は自動テスト結果から推定しない。

不快感があればpause・退出し、その状況を記録する。強い明るさ、ヘッドフォン、片目を隠すこと、特殊な目の状態は要求しない。短い録画を使う場合は指の操作説明を添え、URL/QR/account識別子を映さない。アプリ内pauseと開発ランチャーのアイコンを区別する。

### 表示できない場合

準備中/失敗/一時停止から「描画の診断」→「診断をコピー」。R3Fの箱・床・壁、それも見えない時だけ生のGLの橙色三角形を確認する。「探索へ戻る（進行を維持）」で戻る。手動再試行は最大2回。取得した診断と実際の画面を分けて記録する。

[検証済み範囲](GOAL-006.md)・[35枚の静止表示QA](qa-goal006/README.md)・[知覚との区別](ILLUSION-EVIDENCE.md)を参照。Goal005の過去検証は [原本照合記録](GOAL-005-KIT-VERIFICATION.md)に保持している。

## Goal 012: 任意の呼び鈴と手動仕切り

この節は実機でこれから確認する手順であり、現時点の実施記録ではない。診断 revision は `goal-012-stage-kit-r1`。既存 Development Build の native 依存・設定は変更していないが、端末上の binary とこの JavaScript の組合せは未確認。

1. 影の映写室で灯りを固定し、任意装置を一つも触らず主通路を通って防火幕と出口を完了する。鈴・仕切りが必須条件になっていないことを確認する。
2. 西側の呼び鈴 A と東側の呼び鈴 B をそれぞれ中央に捉え、表示名、押下、再使用待ちを確認する。音が鳴るのは操作板ではなく各受鈴器。敵が直接こちらを見ている時と遮蔽時、標準と控えめ、消音時を別々に観察し、敵の最終調査先が A/B で変わるかを記録する。
3. 西側通路の手動仕切りを南側から閉め、動く板と当たり判定と視線遮断を試す。閉鎖線に立っている時に挟まれないこと、東側の回り道が残ること、北側から開け直せることを確かめる。敵が最後に見た場所を調べる動きも観察する。
4. 操作直後の pause、背景移行、ホーム退出、再入場、章切替、描画エラー後の最大2回 retry を試す。提示前の音が復帰時にまとめて鳴らず、同一セッションの cooldown は続き、コールド再開では任意の仕切りが開く。既存の解錠・発見・脱出と他章の保存が残ることを確かめる。
5. VoiceOver、簡単操作、通常の指操作、左右手、320〜430pt、fontScale 1〜2 を比較する。操作できる表示と実際の command 受理が一致するかを記録する。10回再入場で renderer、GPU資源、音 player、FPS、発熱を実測し、ソフトウェア WebGL の動画数値で代用しない。

## Goal 014.1: 音の引き継ぎと日本語画面

`DEVICE_ACCEPTANCE=PENDING`、`RELEASE_READY=false`。これは実機でこれから行う手順であり、実施記録ではない。今回のローカル修正・全体検証が完了した後、`/home/mhirotaka/workspace/chroma-rift-goal012` の `fix/goal-014-1-audio-and-player-ui` から新しい同梱previewを作成する。作成・署名・インストールはこの自動作業では実行しない。

```bash
cd /home/mhirotaka/workspace/chroma-rift-goal012 &&
npx eas-cli@latest build --platform ios --profile preview
```

新しいJavaScriptと表示文言をインストール済みpreviewへ届けるための更新である。native依存や設定の変更が不要であることと、同梱previewを更新せずに新しいコードが動くことは別。previewはホームアイコンから直接起動する。Metroを起動して同梱previewを更新する手順にはしない。

1. **記録を残して更新する。** 既存のプレイ記録と音設定を消さない。端末、iOS版、消音スイッチ、音量、スピーカー／有線／Bluetoothの出力先を控える。まず出力先と設定を一定にしたまま比較する。
2. **新しいコードを確認する。** ホームには技術情報を常設しない。`設定 → サポート → 詳しい情報` で `goal-014-1-audio-ja-r1`、`profileMarker: preview`、native buildを確認する。必要なら `情報をコピー` を押す。閉じた後、通常表示とVoiceOverの対象からraw情報が消えることを確認する。
3. **自然な遷移で確認する。** 既存保存を消さず、可能な開始位置から01→02→03、続いて04→05→屋外→結末を通す。試験用の再生ボタンやwarpで本編の確認を代用しない。各エリアで以下の3欄を別々に記録する。「play要求があった」という診断だけで聞こえた判定にしない。

| 場所 | 音楽（実際に聞こえた／無音／未確認） | 環境音 | 効果音・操作と時刻 |
|---|---|---|---|
| ホーム |  | 対象外 | 対象外 |
| 01 閉館後の展示室 |  |  |  |
| 02 測れない収蔵庫 |  |  |  |
| 03 影の映写室 |  |  |  |
| 04 鏡越しの回廊 |  |  |  |
| 05 退館制御室 |  |  |  |
| 屋外・結末 |  |  |  |

エリア移動や曲変更の短いfade、ミュート、音量0、意図された静けさを「復旧しない無音」と分ける。退館制御室では停止後の室内環境音が静まり、屋外へ出ると外気へ変わる。結末曲は有限の余韻で、全場面に常時同じ曲を追加していない。

4. **同じ保存から再開する。** 02からの直接再開、可能なら03〜05も確認する。一時停止→再開、設定・発見メモの開閉、音off/on、音楽だけ0／環境音だけ0／効果音だけ0／全音量0を分ける。設定が勝手に戻らず、移動できない壁へ押しているだけで足音が続かず、復帰時に昔の一発音がまとめて鳴らないことを確認する。
5. **中断と出力先を確認する。** 背景へ移すと音が止まり、戻って明示的に再開できることを確認する。別試行で出力先の切断・OS音声中断を試す。切断直後、操作していないのに音が勝手に大きく鳴り直さないことを確認する。サイレントスイッチを尊重するため、消音中に聞こえないことだけを不具合としない。
6. **表示を確認する。** 小さい端末幅と大きい文字でホーム、5エリアの目標・操作・未達理由、一時停止、設定、ノート、エラー、結末を確認する。日本語の長いボタンが切れず、スクロールして操作でき、通常画面にcode／GL／checkpoint等が出ないことを確認する。VoiceOverではボタン名と意味付き操作、詳細を閉じた後の読み上げも確認する。
7. **再発時は実データを採る。** 音楽・環境音・効果音のどれが止まったか、エリア、直前の操作、消音・出力先、直接再開か連続移動かを記録し、できるだけそのままサポートから情報をコピーする。画面の再試行上限やホーム復帰後も保持した最初の失敗を取得できる。コピーは自動送信されない。fixtureや推測した診断を実機記録として提出しない。

ローカルの共有音声セッションfake、Software WebGL/CSS、音声コマンドからの再構成音声は、iPhoneの実聴・Yogaレイアウト・VoiceOver・錯視・恐怖・FPS／発熱の合格証拠ではない。結果は [Goal014.1報告](GOAL-014-1.md) のローカル検証と分けて記録する。
