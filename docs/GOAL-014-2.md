# Goal 014.2 — readable mechanisms and fair escape

Status: implementation and local verification complete; preview/device acceptance remains pending. `DEVICE_ACCEPTANCE=PENDING`, `RELEASE_READY=false`.

## Baseline and evidence scope

- Worktree: `/home/mhirotaka/workspace/chroma-rift-goal012`.
- Branch: `fix/goal-014-2-readable-fair-escape`.
- Starting HEAD: `5bd34f26b65558e8da1cffd9d22ee93c0db78258`; clean start. Original and completion worktrees were left unchanged.
- Installed Node `24.20.0`, npm `11.19.0`. Fresh `npm run check`: exit 0, **134 suites / 1,327 tests**, lint/typecheck/iOS export passed in 375.329 seconds. The 532 tracked source/config/support files were unchanged throughout the baseline run.
- Fresh r8 software framebuffer regression: exit 0. This exercises Chromium/SwiftShader, current Three, mirror, default-framebuffer adapter and diagnostic observer. It is not an iPhone/native presentation result.
- `npm ls --depth=0 --json`: exit 0. Current Expo check and Doctor: exit 1, **57.0.22 installed / ~57.0.23 recommended**, Doctor 20/21. No dependency update was made.
- Fresh npm audit: exit 1, **11 moderate, 0 high, 0 critical**. Its dependency path is the `uuid` bounds-check advisory through `xcode` and Expo tooling; exact current output is retained under `qa-goal014-2/environment/`. No audit fix was applied.
- Baseline hashes, command exit records and logs: [qa-goal014-2](qa-goal014-2/).

The user's current play report is accepted as a problem report. No new personal recording was supplied. Code reproductions and simulations below do not identify the exact sequence on that device.

## A — passage, collision and released input

Confirmed before repair:

- The drawn vestibule extended to z=31.5, but supported floor ended at z=24. A real player-radius walk stopped around z=23.74.
- The final tooth left `holding='winch'`; a retained movement contact had lost ownership and new motion was ignored after release.
- An actor AABB corner intersected the player at diagonal offsets (.53,.53), although center distance exceeded the capture distance. The unsafe-start guard rejected retreat too.
- The old actor work fixture at (-2.45,11.3) intersected `winch-core`.

The repair keeps the visible vestibule and extends its actual floor/side walls. A real 1.5 m doorway at z=31.38 leads to the completion threshold z=31.5; walking across it completes 04. The old exit command cannot remotely complete at z=23. Raising the gate never teleports the player.

The grate's movement envelope remains solid until its lower edge clears the 1.82 m player height. Visible bars are separate opaque LOS volumes; gaps are not an opaque sheet. The third committed tooth releases the hold automatically while its final lift continues. Old end/cancel events are inert; they cannot start another action.

Only the authored mirror actor opts into circular dynamic collision. Static wall, floor and gate guards remain strict. A pre-existing dynamic overlap can move only when every remaining penetration decreases; movement into another body or static obstacle is rejected. No automatic position correction is introduced.

A normal mechanism hold retains contact identities and last positions, discards old vectors, then rearms a retained movement/look finger only on fresh motion. Capture and lifecycle cancellation retain the existing full-release barrier. Presented-recovery capability is introduced as a host contract; the actor reset using it belongs to step B.

Checkpoint schema/key stay unchanged. All five original authorized poses are still accepted. New work checkpoints use the proven shelf pose (-4.05,10.7), facing its south opening. `gateCrossed` records actual passage monotonically; old raw POST_GATE/cleared checkpoints infer it, while contradictory new fields are rejected. Restoring valid old raw data needs no destructive migration or reset.

Validation so far: P0 RED recorded five failures; focused domain checks passed 94 tests and save/stage/campaign checks passed 174 tests. Input RED recorded four failures; current touch/hold suites passed 23 tests. Old completed-fixture expectations now explicitly include their earned gate crossing; assertions were not removed. Initial test-authoring mistakes and intermediate failures remain in `.expo/goal014-2/`, separate from product RED evidence.

## B — reachable investigation, fair retreat and presented recovery

The actor keeps the actual heard point as `lastHeard`, selects a reachable body-sized observation point near the mechanism, and walks around the core using physical corner edges. It does not receive the concealed player's live position. The local investigation allowance is 10 seconds; common `VAULT_AI` speeds and other areas are unchanged. Attacks retain their fixed observed target.

Both rack openings are .75 m wide: the .48 m player fits, the .88 m actor does not. The rendered rack and collision share the same bounds. This shelter uses ordinary walls, body width and LOS, with no hidden invulnerability zone. Its two exits are tested. The escape route releases the winch, walks around the rack's south end, waits under cover until emitted footsteps recede, returns to work, then uses the east side of the corridor and the grate opening.

At an actual attack capture, a single explicit recovery boundary resets actor position/memory, pending sounds and partial hold. Key, practice, committed teeth, discoveries, intensity and gate crossing survive. Before practice the player returns to the entrance; during work to (-4.05,10.7); after actual grate crossing to its far side. The shelf yaw now points southeast at its physical opening; the earlier yaw=0 shelf checkpoint remains accepted verbatim.

The host freezes simulation during the subdued, non-flashing recovery view. Its 1.2-second interval starts only after a successful presentation; pause, background, failed frames and an unreleased old contact cannot spend actor grace. Input-ready releases the pending actor, and grace starts on the following real update. This is a simulated presentation contract, not a measured native load time. A stable scene ancestor observes end/cancel for an action view replaced during capture, without acquiring pointers or adding motion. Viewport changes cancel partial hold; VoiceOver activate uses the same start/stop command.

The measured comparison covers three legal work positions, three entrance waits, three reaction delays and 30/60/variable dt (81 routes). A cue is an actually emitted footplant within 4.5 m; return follows receding emitted footsteps beyond 5.6 m. These QA perception thresholds are not calibrated human hearing. Input decisions do not inspect secret enemy phase, timers or memory. The required .75 and 1.25-second cases retain strict zero-capture assertions. The 1.75-second cases measure either completion or a legal telegraphed capture with preserved progress and safe recovery; blocked movement, unsafe recovery or rework failure still fail. The earlier all-success expectation and its one late-reaction failure are retained as intermediate evidence.

## C — physical mechanism readability

Practice now shows a large grip, winding drum, contacting rope/pulleys, a short weight and its resting support. The main handle connects by cable and pulleys to the grate. Separate bars, vertical rails and three physical pawls expose its opening and committed progress. The visible door is an open frame at the actual completion threshold. The mirror is wider and angled toward the approach; camera/FOV and actor simulation are unchanged.

The same limited treatment adds tray handles/seating and disc pivots in01, distinct movable/fixed parts and a confirming clamp in02, a visible mechanism connection in03, and distinct key/bell/lever/disconnect fittings in05. Existing optical targets, answers, bell instances and partition behavior remain protected by their original tests. Text-hidden QA does not remove production Japanese HUD or accessibility labels.

## D — integration and evidence boundaries

Seven actual App/Screen routes use rendered touch callbacks, pointer adapters, controller/world and real checkpoint/campaign writes: standard direct; subdued; delayed one-tooth retreat; retained left finger; actual partial-progress capture/recovery; actual post-grate capture/recovery; and lifecycle/handedness/VoiceOver. Successful route code never writes pose, ratchets, AI state, clear flags or campaign completion. The seed is a valid fresh04 checkpoint after completed01–03. Separate existing whole-chapter tests cover actual03 entry, replay independence and ending.

Native GPU readiness is an explicit boundary fixture in these Screen integration tests. Separate software WebGL captures render the actual Three scene and planar mirror. Neither establishes physical RN responder arbitration, a person's understanding, audibility, fear, device FPS or heat.

Final full-regression totals, canonical visual/video paths, hashes and command results are retained below. `DEVICE_ACCEPTANCE=PENDING / RELEASE_READY=false` remains the release decision.

## Final regression and source correspondence

`npm run check` completed with child exit0: **142 suites /1,472 tests**, lint, typecheck and iOS export passed. Jest took542.675s; the combined command took634.674s. Baseline was134 suites /1,327 tests. No test is skipped to obtain this result.

The run guarded538 files. All535 production/test/support/asset/config and unchanged-tool entries remained byte-identical; three QA tools changed during the run (common-tape archive transport/metadata and the obsolete mirror-backside fixture). The all-file guard therefore correctly reports false. Latest whole-repository ESLint and the affected full captures subsequently passed, and every current file matches the after-hash list. This is not a claim that all538 files were frozen for the entire command. The outer exec observation returned143 after the complete child result; the npm child recorded0 and the preserved terminal log contains the142/1472 result and successful export. Exact boundaries are in [verification.json](qa-goal014-2/final/verification.json).

The final fairness suite passes87 checks. Its81 comparison rows are **.75s:27/27 wins;1.25s:27/27 wins;1.75s:26/27 wins plus1 legal capture**. The late capture occurred after actual release and movement in the30Hz,9s-wait,western-work case, preserved two teeth and recovered safely. It is a measured delayed-reaction limit, not a blocked-input failure. The required1.25s routes take1.247–1.509s from release to cover and45.099–58.740s end-to-end including entrance waits. [Full matrix and retained attempts](qa-goal014-2/actor-fairness/final.json).

## Current verified command results

- Runtime dependency scan: **iOS / Android / neutral each253 production modules,769 runtime edges,0 SCCs,0 errors**.
- Stage definition validator: both live-definition/start/restore checks pass.
- Actual development and production Metro exports pass. All embedded first-party sources match disk (245 development,225 production); emitted application and R3F factories use identical Three classes in both modes. Six actual development entry orders pass without a first-party cycle. The release export contains all five campaign areas and31 assets, excludes development screens/probe/dev-client JS, and verifies every packaged asset exists.
- Real standard-intensity controller/campaign/audio-owner route completes all five areas and outdoor ending in155.117 simulated seconds. Source/asset guards pass (116 sources,28 audio assets); active players after disposal=0. Ready/seek is a zero-latency native fixture; no listening or native audio acceptance is claimed.
- r8 software framebuffer:19 cases /14 gates pass, including10 fresh entries. The first current run failed only the old backside-camera expectation after the mirror angle changed. The QA camera now stands in a legal actual back-plane position and asserts negative signed plane distance; no rendering assertion was removed. The failing attempt remains recorded.
- Latest independent native lifecycle suite:75/75. Mechanism/body-collider focus:25/25. These complement the full candidate run rather than replacing it.
- Final `npm ls --depth=0 --json`:0. Final Doctor:20/21, exit1. Final Expo dependency check:exit1, installed57.0.22 versus recommended~57.0.23. Final audit:exit1,11 moderate /0 high /0 critical. These are fresh results; dependency/config bytes remain unchanged.
- All six protected files match the starting SHA-256 byte for byte. No native dependency, SDK, Metro, signing or EAS configuration was changed.

Exact outputs are in [final QA records](qa-goal014-2/final/). Text copies trim trailing whitespace only; raw run files remain in `.expo/goal014-2/final/`.

## Visual conclusions and limits

The final matched static set has26 legal viewpoints per revision, each in normal/text-hidden mode:104 PNGs,52 exact camera/state comparisons. Sources/assets/tools are guarded. Middle/east work views gain actual reflected actor pixels; the broad west framing has no reflected actor pixels and uses the direct visible actor instead. This does not establish that all people will notice danger at every view. The recovery facing shows the wall/rack edge and a partial floor opening around it, not an unobstructed corridor view. First-time comprehension remains a device/user check.

Current actual Screen videos retain every sampled10fps frame from60Hz input simulation: straight305 frames /30.5s, retreat491 /49.1s, capture443 /44.3s. They are silent software-render/CSS recordings; state-level audio traces are separate. Straight enters actual05 at28.950s. Retreat hears its modeled cue10.750s, releases12.000s, moves12.033s, reaches cover13.283s and enters05 at47.583s. Capture is confirmed/presented23.267s, input-ready24.467s, and05 entered42.767s. The recovery interval is1.200s. All three save actual05 with key/progress retained and dispose their owners.

The common-input before/after04 clip has166 frames /33.2s: baseline stalls atz23.745; candidate reachesz31.511. Its isolated controller does not perform App handoff, so the final forward image stays in04 beyond its door; the separate actualScreen clip demonstrates05. The05 comparison has125 frames /25s, with all sampled poses matching. Four companion movies use offline mixes from each revision's actual audio-owner trace; these are not native recordings or proof of audibility.

Visual review consists of inspected PNGs/keyframes and automated full-frame render/decode checks. No human continuous playthrough, continuous watching or real listening is claimed. Raw personal footage was not added.

## Local commits and preview handoff

- A `a1d682d`: physical vestibule/grate exit, dynamic body escape, hold-pointer release, baseline/P0 records.
- B `bddc797`: reachable investigation, shelter dimensions, progress-aware presented recovery, fair-route/input regressions.
- C `2f8a097`: physical mechanism/mirror/doorway visuals and targeted scene/native tests.
- D: integration routes, QA tools/media/manifests, support marker and final validation/handoff documentation (this report is part of that commit; obtain its hash with `git log -1`).

Worktree remains `/home/mhirotaka/workspace/chroma-rift-goal012`, branch `fix/goal-014-2-readable-fair-escape`. No push, PR, cloud build, auth/signing change or upload was performed. Dependencies/native configuration are unchanged, but this deployment uses an embedded preview: the user must build and update-install it to receive the new JS/assets. Native build number/EAS ID are unknown until that build completes.

```bash
cd /home/mhirotaka/workspace/chroma-rift-goal012 &&
npx eas-cli@latest build --platform ios --profile preview
```

Launch from the home icon, verify `goal-014-2-fair-escape-r1` under Settings → Support → Details, and continue the existing04 save. Do not delete the app/data or restart01. See the current first section of [iPhone validation](IPHONE_VALIDATION.md). Device rendering/touch/VoiceOver, real sound, first-time understanding, fear and FPS/heat remain unverified: **DEVICE_ACCEPTANCE=PENDING / RELEASE_READY=false**.
