# Goal 014.2 — readable mechanisms and fair escape

Status: implementation and integration verification in progress. `DEVICE_ACCEPTANCE=PENDING`, `RELEASE_READY=false`.

## Baseline and evidence scope

- Worktree: `/home/mhirotaka/workspace/chroma-rift-goal012`.
- Branch: `fix/goal-014-2-readable-fair-escape`.
- Starting HEAD: `5bd34f26b65558e8da1cffd9d22ee93c0db78258`; clean start. Original and completion worktrees were left unchanged.
- Installed Node `24.20.0`, npm `11.19.0`. Fresh `npm run check`: exit 0, **134 suites / 1,327 tests**, lint/typecheck/iOS export passed in 375.329 seconds. The 532 tracked source/config/support files were unchanged throughout the baseline run.
- Fresh r8 software framebuffer regression: exit 0. This exercises Chromium/SwiftShader, current Three, mirror, default-framebuffer adapter and diagnostic observer. It is not an iPhone/native presentation result.
- `npm ls --depth=0 --json`: exit 0. Current Expo check and Doctor: exit 1, **57.0.22 installed / ~57.0.23 recommended**, Doctor 20/21. No dependency update was made.
- Fresh npm audit: exit 1, **11 moderate, 0 high, 0 critical**. Its dependency path is the `uuid` bounds-check advisory through `xcode` and Expo tooling; exact current output is retained under `qa-goal014-2/environment/`. No audit fix was applied.
- Baseline hashes, command exit records and logs: [qa-goal014-2](qa-goal014-2/).

The user's current play report is accepted as a problem report. No new personal recording was supplied. Code reproductions and simulations below do not identify the exact sequence on that device.

## A — passage, collision and released input

Confirmed before repair:

- The drawn vestibule extended to z=31.5, but supported floor ended at z=24. A real player-radius walk stopped around z=23.74.
- The final tooth left `holding='winch'`; a retained movement contact had lost ownership and new motion was ignored after release.
- An actor AABB corner intersected the player at diagonal offsets (.53,.53), although center distance exceeded the capture distance. The unsafe-start guard rejected retreat too.
- The old actor work fixture at (-2.45,11.3) intersected `winch-core`.

The repair keeps the visible vestibule and extends its actual floor/side walls. A real 1.5 m doorway at z=31.38 leads to the completion threshold z=31.5; walking across it completes 04. The old exit command cannot remotely complete at z=23. Raising the gate never teleports the player.

The grate's movement envelope remains solid until its lower edge clears the 1.82 m player height. Visible bars are separate opaque LOS volumes; gaps are not an opaque sheet. The third committed tooth releases the hold automatically while its final lift continues. Old end/cancel events are inert; they cannot start another action.

Only the authored mirror actor opts into circular dynamic collision. Static wall, floor and gate guards remain strict. A pre-existing dynamic overlap can move only when every remaining penetration decreases; movement into another body or static obstacle is rejected. No automatic position correction is introduced.

A normal mechanism hold retains contact identities and last positions, discards old vectors, then rearms a retained movement/look finger only on fresh motion. Capture and lifecycle cancellation retain the existing full-release barrier. Presented-recovery capability is introduced as a host contract; the actor reset using it belongs to step B.

Checkpoint schema/key stay unchanged. All five original authorized poses are still accepted. New work checkpoints use the proven shelf pose (-4.05,10.7), facing its south opening. `gateCrossed` records actual passage monotonically; old raw POST_GATE/cleared checkpoints infer it, while contradictory new fields are rejected. Restoring valid old raw data needs no destructive migration or reset.

Validation so far: P0 RED recorded five failures; focused domain checks passed 94 tests and save/stage/campaign checks passed 174 tests. Input RED recorded four failures; current touch/hold suites passed 23 tests. Old completed-fixture expectations now explicitly include their earned gate crossing; assertions were not removed. Initial test-authoring mistakes and intermediate failures remain in `.expo/goal014-2/`, separate from product RED evidence.

## Remaining work

Actor recovery/fairness, physical mechanism visuals, complete Screen/input/campaign routes, same-input visual comparisons, final full regression and iPhone handoff are still in progress. No local simulation establishes first-time understanding, physical touch arbitration, audibility, fear, device FPS or heat.
