import appConfig from '../../app.json';

/** Product copy follows the same display name that CNG places on the icon.
 * Technical identifiers, save keys and third-party credits are independent. */
export const APP_NAME = appConfig.expo.name;
export const APP_NAME_READING = 'さくしかん';
